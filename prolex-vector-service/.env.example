# Database Configuration
DATABASE_URL=postgres://prolex:password@localhost:5432/prolex_vectors

# Server Configuration
PORT=3000
NODE_ENV=development

# Embedding Configuration
EMBEDDING_MODEL=mock
# EMBEDDING_MODEL=claude  # Pour utiliser Claude plus tard
# ANTHROPIC_API_KEY=sk-ant-xxx  # Clé API Anthropic pour Claude embeddings

# CORS Configuration (optionnel)
# CORS_ORIGIN=http://localhost:3000

# Logging
LOG_LEVEL=info
