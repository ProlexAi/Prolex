# ===========================================
# .env.example
# ===========================================
#
# Fichier d'exemple pour les variables d'environnement.
# Copier ce fichier en .env et ajuster les valeurs selon votre configuration.
#
# USAGE :
# cp .env.example .env
# nano .env  # Éditer le fichier
#

# ===== Configuration de base =====
PORT=8080
HOST=0.0.0.0
NODE_ENV=development

# ===== Authentification =====
# Token d'authentification Bearer
# ⚠️ IMPORTANT : Définir un token fort en production !
# Si non défini : authentification désactivée (OK pour dev local)
# DASHBOARD_BASIC_AUTH_TOKEN=change_me_to_a_strong_secret_token_123456789

# ===== Sécurité - Exec =====
# Désactiver la fonctionnalité d'exécution de commandes
# Valeurs : 1 (désactivé) | 0 (activé)
# ⚠️ Toujours mettre à 1 en production !
DISABLE_EXEC=0

# ===== Docker Socket =====
# Chemin vers le socket Docker
# Linux/WSL : /var/run/docker.sock
# Docker Desktop (Mac) : /var/run/docker.sock.raw (parfois)
DOCKER_SOCKET=/var/run/docker.sock
