# ═══════════════════════════════════════════════════════════════════
# 🔐 PROLEX - Configuration Centralisée des Variables d'Environnement
# ═══════════════════════════════════════════════════════════════════
#
# ⚠️ IMPORTANT :
# 1. Copier ce fichier :
#    - Pour LOCAL : cp .env.example .env.local
#    - Pour VPS   : cp .env.example .env.vps
# 2. Remplacer toutes les valeurs vides par vos vraies clés
# 3. NE JAMAIS versionner .env.local ou .env.vps (déjà dans .gitignore)
#
# 🔑 Génération de secrets sécurisés :
#    openssl rand -base64 32    # Pour clés de 32 caractères
#    openssl rand -base64 24    # Pour mots de passe
#
# 📖 Documentation : voir config/README.md
#
# ═══════════════════════════════════════════════════════════════════

# ───────────────────────────────────────────────────────────────────
# 🌍 ENVIRONNEMENT GLOBAL
# ───────────────────────────────────────────────────────────────────

# Environnement de déploiement : local | vps | test
# Utilisé par config-loader.ts pour charger le bon fichier .env
PROLEX_ENV=local

# Environnement Node.js : development | staging | production
NODE_ENV=development

# Domaine racine (VPS uniquement)
DOMAIN_ROOT=iaproject.cloud

# ───────────────────────────────────────────────────────────────────
# 🔄 N8N - Workflow Automation
# ───────────────────────────────────────────────────────────────────

# URL de base n8n (avec protocole)
# Local  : http://localhost:5678
# VPS    : https://n8n.iaproject.cloud
N8N_BASE_URL=http://localhost:5678

# Clé API n8n
# À générer depuis l'interface n8n : Settings > API > Generate API Key
N8N_API_KEY=

# Timeout pour les requêtes n8n (ms)
N8N_TIMEOUT=30000

# Configuration VPS n8n (optionnel en local)
N8N_HOST=n8n.iaproject.cloud
N8N_PORT=5678
N8N_PROTOCOL=https

# Clé de chiffrement pour les credentials n8n (OBLIGATOIRE en VPS)
# ⚠️ TRÈS IMPORTANT : Générer avec : openssl rand -base64 32
# ⚠️ NE JAMAIS CHANGER cette clé après le premier démarrage !
N8N_ENCRYPTION_KEY=

# Authentification basique n8n (optionnel)
N8N_BASIC_AUTH_ACTIVE=false
N8N_BASIC_AUTH_USER=
N8N_BASIC_AUTH_PASSWORD=

# ───────────────────────────────────────────────────────────────────
# 🤖 LLM APIs - OpenAI, Anthropic, etc.
# ───────────────────────────────────────────────────────────────────

# OpenAI (GPT-4, GPT-3.5, etc.)
OPENAI_API_KEY=

# Anthropic (Claude)
ANTHROPIC_API_KEY=

# Provider LLM par défaut (openai | anthropic | ollama)
LLM_PROVIDER=anthropic

# ───────────────────────────────────────────────────────────────────
# 🧠 ANYTHINGLLM - Plateforme d'agents IA
# ───────────────────────────────────────────────────────────────────

# Domaine public d'AnythingLLM
ANYTHINGLLM_HOST=llm.iaproject.cloud

# Token d'authentification pour accéder à l'interface AnythingLLM
# Générer avec : openssl rand -base64 24
ANYTHINGLLM_AUTH_TOKEN=

# ───────────────────────────────────────────────────────────────────
# 🐘 POSTGRESQL - Base de données
# ───────────────────────────────────────────────────────────────────

# URL de connexion PostgreSQL complète
# Format: postgres://USER:PASSWORD@HOST:PORT/DATABASE
# Local  : postgres://prolex:password@localhost:5432/prolex_db
# VPS    : postgres://prolex:password@prolex-postgres:5432/prolex_db
DATABASE_URL=postgres://prolex:password@localhost:5432/prolex_db

# Configuration détaillée PostgreSQL (optionnel, pour services qui n'utilisent pas DATABASE_URL)
POSTGRES_USER=prolex
POSTGRES_PASSWORD=
POSTGRES_DB=prolex_db
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# ───────────────────────────────────────────────────────────────────
# 🔴 REDIS - Cache & Queues (OPTIONNEL)
# ───────────────────────────────────────────────────────────────────

# URL de connexion Redis (optionnel)
# Format: redis://[password@]host:port
REDIS_URL=redis://localhost:6379

# Mot de passe Redis (si authentification activée)
REDIS_PASSWORD=

# ───────────────────────────────────────────────────────────────────
# 📊 GOOGLE SERVICES - Sheets, Drive, etc.
# ───────────────────────────────────────────────────────────────────

# Chemin vers le fichier de credentials Google
GOOGLE_SHEETS_CREDENTIALS_PATH=./credentials/google-sheets-key.json

# ID du spreadsheet SystemJournal (logs système)
SYSTEM_JOURNAL_SPREADSHEET_ID=1xEEtkiRFLYvOc0lmK2V6xJyw5jUeye80rqcqjQ2vTpk

# Activer les logs vers SystemJournal
SYSTEM_JOURNAL_ENABLED=true

# ID du spreadsheet comptabilité (MCP Finance)
COMPTABILITE_SPREADSHEET_ID=

# ───────────────────────────────────────────────────────────────────
# 💳 STRIPE - Paiements (MCP Finance)
# ───────────────────────────────────────────────────────────────────

# Clés Stripe (test ou production)
STRIPE_SECRET_KEY=sk_test_
STRIPE_PUBLISHABLE_KEY=pk_test_
STRIPE_WEBHOOK_SECRET=whsec_

# ───────────────────────────────────────────────────────────────────
# 💰 PAYPAL - Paiements alternatifs (MCP Finance)
# ───────────────────────────────────────────────────────────────────

PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=
PAYPAL_MODE=sandbox

# ───────────────────────────────────────────────────────────────────
# 🏦 PLAID - Connexion bancaire (MCP Finance, optionnel)
# ───────────────────────────────────────────────────────────────────

PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox

# ───────────────────────────────────────────────────────────────────
# 🪙 CRYPTO APIs (MCP Finance, optionnel)
# ───────────────────────────────────────────────────────────────────

# CoinGecko API (gratuit)
COINGECKO_API_KEY=

# Binance API (trading)
BINANCE_API_KEY=
BINANCE_API_SECRET=

# ───────────────────────────────────────────────────────────────────
# 📧 EMAIL - Gmail API ou SMTP (MCP Communication)
# ───────────────────────────────────────────────────────────────────

# Gmail API (recommandé)
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
GMAIL_REFRESH_TOKEN=
GMAIL_USER_EMAIL=

# OU SMTP (alternatif)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=

# Expéditeur par défaut
EMAIL_FROM=Prolex AI <noreply@automatt.ai>

# ───────────────────────────────────────────────────────────────────
# 📱 SMS & WHATSAPP - Twilio (MCP Communication)
# ───────────────────────────────────────────────────────────────────

TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
TWILIO_WHATSAPP_NUMBER=

# ───────────────────────────────────────────────────────────────────
# 💬 SLACK (MCP Communication)
# ───────────────────────────────────────────────────────────────────

SLACK_BOT_TOKEN=
SLACK_APP_TOKEN=
SLACK_SIGNING_SECRET=
SLACK_DEFAULT_CHANNEL=#general

# ───────────────────────────────────────────────────────────────────
# 📞 TELEGRAM (MCP Communication)
# ───────────────────────────────────────────────────────────────────

TELEGRAM_BOT_TOKEN=
TELEGRAM_DEFAULT_CHAT_ID=

# Email d'alerte admin (pour notifications critiques)
ADMIN_EMAIL=matthieu@automatt.ai
ADMIN_TELEGRAM_CHAT_ID=

# ───────────────────────────────────────────────────────────────────
# 🔒 SÉCURITÉ - Whitelists & Blacklists (MCP Communication)
# ───────────────────────────────────────────────────────────────────

# Whitelist de destinataires autorisés (séparés par virgules)
ALLOWED_EMAIL_RECIPIENTS=
ALLOWED_PHONE_NUMBERS=
ALLOWED_SLACK_USERS=
ALLOWED_TELEGRAM_CHATS=

# Domaines email autorisés (wildcards supportés)
ALLOWED_EMAIL_DOMAINS=automatt.ai

# Blacklist (contacts bloqués)
BLOCKED_RECIPIENTS=

# Confirmation requise pour contacts hors whitelist
REQUIRE_CONFIRMATION_NON_WHITELISTED=true

# ───────────────────────────────────────────────────────────────────
# 🛡️ RATE LIMITING - Protection contre abus
# ───────────────────────────────────────────────────────────────────

# MCP Communication - Limites par canal (par heure)
RATE_LIMIT_EMAIL_PER_HOUR=50
RATE_LIMIT_SMS_PER_HOUR=20
RATE_LIMIT_WHATSAPP_PER_HOUR=30
RATE_LIMIT_SLACK_PER_HOUR=100
RATE_LIMIT_TELEGRAM_PER_HOUR=100
RATE_LIMIT_GLOBAL_PER_HOUR=200

# MCP N8N - Rate limiting
RATE_LIMIT_PER_SECOND=10
RATE_LIMIT_MAX_CONCURRENT=5
RATE_LIMIT_QUEUE_SIZE=100

# ───────────────────────────────────────────────────────────────────
# 🔄 RETRY & RESILIENCE (MCP N8N)
# ───────────────────────────────────────────────────────────────────

RETRY_MAX_ATTEMPTS=3
RETRY_INITIAL_DELAY=1000
RETRY_MAX_DELAY=10000
RETRY_BACKOFF_MULTIPLIER=2
RETRY_FALLBACK_WORKFLOW_ID=

# ───────────────────────────────────────────────────────────────────
# 💾 CACHE Configuration
# ───────────────────────────────────────────────────────────────────

CACHE_ENABLED=true
CACHE_TTL=300
CACHE_TTL_SECONDS=300
CACHE_CHECK_PERIOD=60

# ───────────────────────────────────────────────────────────────────
# 🎯 PROLEX SANDBOX - Environnement de test
# ───────────────────────────────────────────────────────────────────

# URLs n8n pour tests
URL_N8N_TEST=http://localhost:5678
URL_N8N_PROD=https://n8n.automatt.ai

# Mode Sandbox : strict | relaxed
# strict  : bloque les actions à risque élevé
# relaxed : simule tout mais alerte
MODE_SANDBOX=strict

# Garde-fous
GARDES_FOUS_ACTIFS=true
MAX_ACTIONS_PAR_TEST=50
BACKUP_OBLIGATOIRE=false

# Base de données Sandbox
DB_TYPE=sqlite
DB_PATH=./sandbox/sandbox.db

# Si postgres pour sandbox (optionnel)
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=prolex_sandbox
# DB_USER=prolex
# DB_PASSWORD=

# Serveur Sandbox
SANDBOX_PORT=3001
SANDBOX_HOST=localhost

# Fichiers Sandbox
FICHIER_SCENARIOS=./sandbox/scenarios.json

# ───────────────────────────────────────────────────────────────────
# 🔢 VECTOR SERVICE - Service de vecteurs embeddings
# ───────────────────────────────────────────────────────────────────

# Port du service de vecteurs
VECTOR_SERVICE_PORT=3000

# Modèle d'embeddings : mock | claude | openai
EMBEDDING_MODEL=mock

# CORS (optionnel)
CORS_ORIGIN=http://localhost:3000

# ───────────────────────────────────────────────────────────────────
# 🔌 MCP SERVERS - Configuration générale
# ───────────────────────────────────────────────────────────────────

# Port du serveur MCP (service interne, pas exposé publiquement)
MCP_SERVER_PORT=3100

# Mode debug (true pour plus de logs)
MCP_DEBUG=false

# Healthcheck port
HEALTHCHECK_PORT=3000

# ───────────────────────────────────────────────────────────────────
# 📝 LOGGING Configuration
# ───────────────────────────────────────────────────────────────────

# Niveau de logs : error | warn | info | debug
LOG_LEVEL=info

# Chemin des fichiers de logs
LOG_PATH=./logs

# Log tous les messages envoyés (MCP Communication)
LOG_ALL_MESSAGES=true

# Logs de sécurité
SECURITY_LOG_ENABLED=true
SECURITY_LOG_PATH=./logs/security.log

# ───────────────────────────────────────────────────────────────────
# 🎬 STREAMING Configuration (MCP N8N)
# ───────────────────────────────────────────────────────────────────

STREAMING_ENABLED=true
STREAMING_POLL_INTERVAL=1000

# ───────────────────────────────────────────────────────────────────
# 🛡️ SÉCURITÉ AVANCÉE
# ───────────────────────────────────────────────────────────────────

# Validation des requêtes
ENABLE_REQUEST_VALIDATION=true

# Limites financières (MCP Finance)
MAX_INVOICE_AMOUNT=50000
REQUIRE_CONFIRMATION_ABOVE=10000

# Confirmation pour envois en masse (MCP Communication)
REQUIRE_CONFIRMATION_BULK_THRESHOLD=10
MAX_ATTACHMENT_SIZE_MB=10

# Détection de menaces (MCP Communication)
ENABLE_THREAT_DETECTION=true
BLOCK_SUSPICIOUS_LINKS=true
ALERT_ON_SUSPICIOUS_ACTIVITY=true

# Scanner pièces jointes avec VirusTotal (optionnel)
VIRUSTOTAL_API_KEY=
SCAN_ATTACHMENTS=false

# Webhook secret (MCP Finance)
WEBHOOK_SECRET=

# Exchange Rate API (MCP Finance)
EXCHANGE_RATE_API_KEY=

# ───────────────────────────────────────────────────────────────────
# 🌐 TRAEFIK - Reverse Proxy (VPS uniquement)
# ───────────────────────────────────────────────────────────────────

# Email pour les certificats SSL Let's Encrypt (OBLIGATOIRE en VPS)
# ⚠️ Mettre un email valide pour recevoir les notifications de renouvellement
TRAEFIK_ACME_EMAIL=

# IP autorisées pour accéder à l'interface (whitelist, optionnel)
# Format : IP1,IP2,IP3 (séparées par virgules)
ALLOWED_IPS=

# ───────────────────────────────────────────────────────────────────
# 🛠️ NOTES POUR LE DÉPLOIEMENT
# ───────────────────────────────────────────────────────────────────

# Après avoir créé et rempli votre fichier .env.local ou .env.vps :
#
# 1. Vérifier les permissions :
#    chmod 600 .env.local
#    chmod 600 .env.vps
#
# 2. Vérifier qu'aucune variable n'est vide (pour les variables obligatoires) :
#    grep -E "^[A-Z_]+=\s*$" .env.local
#
# 3. Lancer les services (selon votre environnement) :
#    npm run dev              # Pour développement local
#    docker compose up -d     # Pour VPS
#
# 4. Vérifier les logs :
#    npm run logs
#    docker compose logs -f
#
# 📖 Pour plus d'informations, voir : config/README.md
